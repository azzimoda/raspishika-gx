# Скрейпер данных МПК ТИУ (`internal/api`)

Документация по всем HTTP-источникам данных, которые использует скрейпер
`internal/api/scraper`. Скрейпер работает **чистым HTTP без браузера**:
отделения, группы и преподаватели получаются POST-запросами к AJAX-эндпоинтам
сайта расписания, а сами расписания (группы и преподаватели) — прямым GET
страниц `sh.php`. Информация собрана в результате реверс-инжиниринга
`https://coworking.tyuiu.ru/shs/`.

---

## 1. Точка входа и кодировки

| Параметр | Значение |
|---|---|
| Страницы-оболочки | `https://coworking.tyuiu.ru/shs/index.php`, `gr_all/groups.php`, `prep_n/prep.php` |
| AJAX-эндпоинты | `https://coworking.tyuiu.ru/shs/gr_all/funct.php`, `https://coworking.tyuiu.ru/shs/prep_n/funct.php` |
| Кодировка | HTML — `windows-1251` (конвертируется `encodeWindows1251ToUTF8`); JSON-ответы — валидный UTF-8 с эскейпами `\u` |
| Протокол | HTTPS, POST `application/x-www-form-urlencoded` / GET |

`funct.php` **не требует cookies, сессий или Referer** — данные отдаются по
простому POST. Страницы-оболочки (groups.php, prep.php) без
`Referer: https://coworking.tyuiu.ru/shs/index.php` отвечают 302 → `index.php`,
но в скрейпере они не нужны.

Рекомендуемые заголовки POST (`scraper.postForm`):

```http
Content-Type: application/x-www-form-urlencoded; charset=UTF-8
X-Requested-With: XMLHttpRequest
User-Agent: <реальный UA браузера>
```

---

## 2. Отделения и группы (`gr_all/funct.php`)

### 2.1. Список отделений

```
POST https://coworking.tyuiu.ru/shs/gr_all/funct.php
Body: action=load_info
```

Ответ — JSON-массив:

```json
[{"un":0,"id":"28728","year":"2026","err":0},
 {"un":0,"id":"28727","year":"2026","err":0}, ...]
```

Записи с `err != 0` игнорируются. `id` — это `vd` (идентификатор отделения),
`year` — текущий учебный год (подставляется в URL расписания). Значения меняются
каждый учебный год — их надо запрашивать в рантайме, не хардкодить.

### 2.2. Группы отделения

```
POST .../gr_all/funct.php
Body: action=load&act=list_groups&otd=0&vd=<id>
```

Ответ — JSON-массив строковых массивов:

```json
[["926","КИПр","25","9","1","otd","АиЭС"],
 ["991","КИПр","26","9","1"],
 ["992","КИПр","26","11","1"], ...]
```

| Индекс | Содержимое | Пример |
|---|---|---|
| `[0]` | внутренний id группы | `"991"` |
| `[1]` | аббревиатура/код | `"КИПр"` |
| `[2]` | год набора (2 цифры, для названия) | `"26"` |
| `[3]` | база (9 или 11 классов) | `"9"` / `"11"` |
| `[4]` | номер группы в потоке | `"1"` |
| `[5]` | маркер «это отделение» — `"otd"` | только у заголовка |
| `[6]` | название отделения | `"АиЭС"` | только у заголовка |

Название группы: `аббревиатура-год-(база)-номер`, например `КИПр-26-(9)-1`.
Мэппинг в `internal/model`:
- `Department{ID, Name, Year}` — отделение (`id`, `r[6]`, год из `load_info`);
- `Group{GroupID: r[0], DepartmentID, GroupName, DepartmentName, Year}`.

---

## 3. Преподаватели (`prep_n/funct.php`)

**Внимание:** `act=list_prepods` на `gr_all/funct.php` возвращает `"e"` (ошибку).
Правильный хост — `prep_n`.

```
POST https://coworking.tyuiu.ru/shs/prep_n/funct.php
Body: action=load&act=list_prepods&otd=0&bs=0
```

Ответ — JSON-массив пар `[id, ФИО]`:

```json
[["273","Абайдулина Ольга Владимировна"],
 ["939","Абатурова Татьяна Николаевна"], ...]
```

Мэппинг в `internal/model`: `Teacher{TeacherID, Name}`. Исторически фронтенд
фильтрует плейсхолдеры вида «Вакансия», «Вакансия 1» — в актуальных данных их
нет, но фильтр оставлен для страховки.

---

## 4. Расписания (`all_t/sh.php`)

### 4.1. Группа — серверный HTML

```
GET .../all_t/sh.php?action=group&union=0&sid=<vd>&gr=<id>&year=<год>&vr=1
```

Ответ `200` — полный HTML с таблицей `<table id="main_table">`, ячейки занятий
отрендерены сервером. Парсится `parseSchedule` после конвертации `cp1251 → UTF-8`.

### 4.2. Преподаватель — скелет-страница + AJAX-наполнение ячеек

```
GET .../all_t/sh.php?action=prep&prep=<id>&vr=1&count=<N>
    &shed[0]=<vd>&union[0]=0&year[0]=<год>
    &shed[1]=<vd>&union[1]=0&year[1]=<год> ...
```

**Грабли №1:** параметры `shed[i]`, `union[i]`, `year[i]` должны передаваться
**с «сырыми» квадратными скобками** (`&shed[0]=...`). Если процентировать их
(`shed%5B0%5D=...`), сервер отвечает **403**. Поэтому URL собирается строкой
через `fmt.Sprintf`, а не через `url.Values.Encode()`.

**Грабли №2:** страница возвращает только «скелет» — даты, время, а ячейки занятий
пустые (`<td class=urok id='ur119'></td>`). Сами занятия подтягиваются JS через
вызовы, вшитые в HTML:

```html
<script>getUrok('273','1','1788202800','1','9');</script>
```

Каждому вызову соответствует запрос:

```
GET .../all_t/Model.php?task=get_urok&format=row&p=<id>&c=<чет/нечет>&r=<unix>
```

где `<c>` — знаменатель/числитель недели (`''` или `'1'`), `<r>` — unix-время дня,
`<dn>`/`<ms>` — день и месяц. Ответ — JSON `{"1": [...], ..., "7": [...]}` по
номерам пар:

```json
"1": ["urok", "Русский язык", "<br><div style=font-size:10px>ЭРЭр-26-(9)-1", "210<\/br>(10)", ""]
"5": ["ekz", "Экзамен", "Дисциплина", "210<\/br>(10)", "", "подгруппа"]
```

`"urok"`-строка: `[1]` дисциплина, `[2]` группа (с HTML-разметкой), `[3]` кабинет,
`[4]` класс-модификатор (`zamena`, `event`, ...). Пустые пары — `["urok","","","",""]`.
`"ekz"`-строка: `[1]` заголовок экзамена, `[2]` дисциплина, `[3]` кабинет,
`[5]` подгруппа.

Скрейпер повторяет логику `getUrok` из `all_t/shed.js` в Go: собирает HTML ячейки
по тому же шаблону (классы `.disc`, `.cabs`, `.podgrupp`, `head_ekz`,
`comm3 <тип>`) и подставляет в скелет по id `#ur<урок><dn><ms>`, после чего
отдаёт результат в `parseSchedule`. Благодаря этому HTML выглядит так же, как
рендер в браузере, и разбор расписания общий для групп и преподавателей.

**Грабли №3:** `Model.php` за JQ противобот-проверкой. Отвечают **403**:
- запросы с «навигационными» заголовками браузера (`Accept-Language`,
  `Upgrade-Insecure-Requests`, `Sec-Fetch-*` и т.п.) в сочетании с Referer;
- «старые» User-Agent (например, `Firefox/3.6`), которые иногда выдаёт
  `uarand`.

Поэтому скрейпер шлёт на `Model.php` только современный UA + Referer
(`scraper.minimalHeaders`, ротация проверенных UA в `modernUserAgents`).

---

## 5. Прочее

- **Каникулы:** `GET /shs/index.php` → `403` означает, что сайт в «режиме
  каникул» (`CheckVacation`). `200` — обычный режим.
- **`ScheduleURL`:** собирает URL расписания по `model.ScheduleConfig`. Для
  преподавателя принимает список `model.Department` (по ним строятся `shed[]`
  и `year[]`); для группы — по ключам `DepartmentID`, `GroupID`, `Year`.
- **Загрузка сторонних библиотек/UI не требуется** — только `net/http` и
  парсеры HTML/JSON внутри проекта.